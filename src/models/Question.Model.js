export default class Question {
    constructor(id, content, price, createdAt, userId) {
      this.id = id;
      this.content = content;
      this.price = price;
      this.createdAt = createdAt;
      this.userId = userId;
    }
  }
  